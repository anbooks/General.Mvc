
@using  General.Framework.Datatable
@using General.Entities
@*@model DataSourceResult<test_JqGrid, test_JqGridSearchArg>*@
@*@model test_JqGrid*@

@model IEnumerable<General.Entities.test_JqGrid>

@{
    ViewData["Title"] = "测试数据加载";

}

<div class="breadcrumbs" id="breadcrumbs">
    <ul class="breadcrumb">
        <li>
            <i class="ace-icon fa fa-home home-icon"></i>
            <a href="@Url.RouteUrl("mainIndex")">首页</a>
        </li>
        <li class="active">测试数据加载</li>
    </ul>
</div>
<div class="page-content">
    <div class="page-header">
        <h1>
            @ViewData["Title"]
        </h1>
    </div>

    <div class="space-2"></div>

    <div class="row">
        <div class="col-sm-12">
            <table id="user_datatable" class="table table-striped table-bordered table-hover dataTable no-footer">
                <thead>
                    <tr>
                        <th width="150px">编号</th>
                        <th width="100px">名字</th>
                    </tr>
                </thead>
                <tbody>
                    @*@foreach (var item in Model.Data)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>

            </tr>
        }*@

                    @*<tr>
                        <td>@Model.Id</td>
                        <td>@Model.Name</td>

                    </tr>*@

                    @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>

            </tr>
        }
                </tbody>
            </table>


        </div>
    </div>
</div>


