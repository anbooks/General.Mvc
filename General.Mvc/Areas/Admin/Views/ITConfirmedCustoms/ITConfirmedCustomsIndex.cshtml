@using General.Framework.Menu
@using General.Framework.Datatable
@using General.Entities
@model DataSourceResult<ImportTrans_main_record, SysCustomizedListSearchArg>

@{
    ViewData["Title"] = "综保报关行";
}

<div class="breadcrumbs" id="breadcrumbs">
    <ul class="breadcrumb">
        <li>
            <i class="ace-icon fa fa-home home-icon"></i>
            <a href="@Url.RouteUrl("mainIndex")">首页</a>
        </li>
        <li>

            <a href="">进口运输</a>
        </li>
        <li class="active">综保报关行</li>
    </ul>
</div>
<div class="page-content">
    <div class="page-header">
        <h1>
            @ViewData["Title"]
        </h1>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <a asp-route="excelConfirmedCustoms" class="btn btn-sm btn-pink">核注清单生成</a>
        </div>
    </div><div class="space-2"></div>
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <form asp-route="itConfirmedCustoms" method="get" class="form-inline">
                        <label>关键字</label>
                        <input type="text" class="form-control" name="itemno" style="width:180px;" value="@Model.Paging.RouteArg.itemno" placeholder="编号" />
                        <input type="text" class="form-control" name="shipper" style="width:180px;" value="@Model.Paging.RouteArg.shipper" placeholder="发货人" />
                        <input type="text" class="form-control" name="pono" style="width:180px;" value="@Model.Paging.RouteArg.pono" placeholder="合同号" />
                        <button type="submit" class="btn btn-purple btn-sm">
                            查询
                            <i class="ace-icon fa fa-search icon-on-right bigger-110"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <form asp-route="itConfirmedCustoms" method="post" id="formitConfirmedCustoms" class="form-horizontal" onsubmit="return false;">
        <div class="panel panel-default">
            <div class="form-inline">
                <button class="btn btn-info btn-sm" type="submit">
                    <i class="ace-icon fa fa-check bigger-110"></i>
                    确认修改
                </button>
            </div>
        </div>
        <div id='hello' style="z-index: 102; left: 240px; overflow: auto; width: 1670px;">
            <div class="form-group">
                @if (Model != null)
                {

                    <table id="user_datatable" class="table table-striped table-bordered table-hover dataTable no-footer"style="margin-left:20px;">
                        <thead>
                            <tr>
                                <th width="60px"></th>
                                <th width="50px"></th>
                                <th width="80px">编号</th>
                                <th width="80px">发货人</th>
                                <th width="80px">合同号</th>
                                <th width="80px">贸易条款</th>
                                <th width="80px">货物类型</th>
                                <th width="80px">实际收/提货日期</th>
                                <th width="80px">件数(箱数)</th>
                                <th width="80px">毛重</th>
                                <th width="80px">运输方式</th>
                                <th width="80px">要求送货日期</th>
                                <th width="80px">核注清单号</th>
                                <th width="80px">是否核放</th>
                                <th>操作</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model.Data)
                            {
                            <tr>
                                <td>
                                    <a href="@Url.RouteUrl("editITConfirmedCustoms", new { id = item.Id })" class="btn btn-xs btn-purple"><i class="fa fa-bolt"></i>修改</a>
                                </td>
                                <td>
                                    <input type="checkbox" name="checkboxId" value="@item.Id" />
                                    <input type="hidden" name="checkboxValue" value="0" />
                                </td>
                                <td>
                                    @item.Id
                                </td>
                                <td>
                                    @item.Shipper
                                </td>
                                <td>@item.PoNo</td>
                                <td>@item.Incoterms</td>
                                <td>@item.CargoType</td>
                                <td>
                                    @item.RealReceivingDate
                                </td>
                                <td>@item.Pcs</td>
                                <td>@item.Gw</td>
                                <td> @item.ShippingMode</td>
                                <td>@item.DeliveryRequiredDate</td>

                                @if (ViewBag.QX == "01")
                                {
                                    <td>
                                        <input type="text" name="InventoryNo" value="@item.InventoryNo" style="width:80px" />
                                    </td>
                                }
                                else
                                {
                                    <td>
                                        <input type="text" name="InventoryNo" value="@item.InventoryNo" disabled style="width:80px" />
                                    </td>
                                }

                                @if (@item.CheckAndPass == null)
                                {
                                    <td>
                                        @Html.DropDownList("CheckAndPass", ViewData["CheckAndPass"] as SelectList, "-是否核放-", new { @class = "form-control  input-small" })

                                    </td>
                                }
                                else
                                {
                                    <td>
                                        @Html.DropDownList("CheckAndPass", ViewData["CheckAndPass"] as SelectList, @item.CheckAndPass, new { @class = "form-control  input-small" })
                                    </td>
                                }
                                @if (@item.F_ShippingModeGiven != true)
                                {
                                    <td>
                                        <a href="@Url.RouteUrl("itConfirmedCustomsSchedule", new { id = item.Id })" class="btn btn-xs btn-danger btnDelete"><i class="fa fa-bolt"></i>查看明细表</a>
                                    </td>
                                }
                                else
                                {
                                    <td>
                                        <a href="@Url.RouteUrl("itConfirmedCustomsSchedule", new { id = item.Id })" class="btn btn-xs btn-purple"><i class="fa fa-bolt"></i>查看明细表</a>
                                    </td>
                                }
                            </tr>
                            }
                        </tbody>
                    </table>
                    <pager page-value="Model.Paging"></pager>

                }
            </div>
        </div>
       
    </form>
    <script>
        $(function () {
            $('#formitConfirmedCustoms').submit(function () {
                var form = $(this);
                var subArray = [];
                $("input[name='checkboxId']:checked").each(function () {
                    var test = $(this).next();
                    var i = 0;
                    var id = $(this).closest("tr").find("td").eq(1).find("input:checkbox");

                    var inventoryNo = $(this).closest("tr").find("td").eq(12).find("input:text");
                    var checkandpass = $(this).closest("tr").find("td").eq(13).find("select[name='CheckAndPass']");
                    var mSub = {
                        id: id.val(),
                        inventoryNo: inventoryNo.val(),
                        checkandpass: checkandpass.val(),
                    };
                    subArray.push(mSub);
                });
                alert(JSON.stringify(subArray));
                var _url = "/admin/itConfirmedCustoms/itConfirmedCustomsList"
                form.ajaxSubmit({
                    type: 'post', // 提交方式 get/post
                    url: _url, // 需要提交的 url
                    dataType: "json", //数据类型 注意这里！！！！！
                    data: { "kevin": JSON.stringify(subArray) },
                    success: function (data) { // data 保存提交后返回的数据，一般为 json 数据
                        // 此处可对 data 作相关处理
                        var rs = data;
                        if (rs.status == true) {
                            alert('提交成功！');
                        } else {
                            alert('提交失败！数据有误！！！');
                            window.parent.location.reload();
                        }
                    }
                });
                return false; // 阻止表单自动提交事件，必须返回false，否则表单会自己再做一次提交操作，并且页面跳转


            });

        });
    </script>


</div>


